type Query {
  movies: [Movie!]!
}

type Mutation {
  createMovie(
    name: String!
    description: String!
    image: String!
    duration: String!
  ): Movie!
}

type Movie {
  id: ID!
  name: String!
  description: String!
  image: String!
  duration: String!
}
