type Query {
  bind: String!
  movies: [Movie!]
}

type Mutation {
  createMovie(
    name: String!
    description: String!
    image: String!
    relaseData: String!
  ): Movie!
}

type Movie {
  id: ID!
  name: String!
  description: String!
  image: String!
  relaseData: String!
}
